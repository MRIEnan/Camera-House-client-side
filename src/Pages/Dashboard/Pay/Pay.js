import React from 'react';
import {Container,Typography} from '@mui/material';

const Pay = () => {
    return (
        <Container sx={{p:2}}>
            <Typography sx={{textAlign:'center'}} variant="h6">This page is in development.</Typography>
        </Container>
    );
};

export default Pay;